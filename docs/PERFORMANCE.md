# Performance and responsiveness tracker

This is the implementation backlog and evidence record for multiplayer performance. It covers the transport, room relay, table replica, shared shop, spectator view, and native UI bridges present in this repository. Stable `PERF-*` and `GAP-*` IDs are intended for follow-up changes and regression reports.

**Current evidence: static audit plus native macOS correctness fixtures.** The bounded screenshot harness exercises native rendering and the scenarios recorded below. No before/after frame-time distributions, network RTTs, payload-size distributions, GPU timings, or live cross-platform sessions were measured. “Implemented; unmeasured” means code and functional checks support the mitigation; it does not mean the lag report is resolved. Selected inherited game methods were also inspected through static decoding of the Ultrapool 0.15.7 resource pack. Native costs, driver behavior, and real Steam routing still need profiling.

## How to maintain this tracker

| Field | Meaning |
| --- | --- |
| Priority | `P1`: first candidates for visible stalls or input delay; `P2`: throughput, repeated work, or transition costs; `P3`: smaller/conditional costs. Priority is a static assessment, not a measured ranking. |
| Evidence | `V`: verified in repository code; `N`: additionally verified from native 0.15.7 script tokens; `H`: plausible cost still to inspect or profile. None is a runtime timing measurement. |
| Status | `Open`; `In progress`; `Partial; unmeasured`; `Implemented; unmeasured`; `Retired: feature removed`; `Verified` only after the acceptance check passes and the evidence is linked. A mitigation can leave a residual task open. |
| Acceptance | A concrete behavior or measurement required to close the item. Runtime acceptance requires the explicit authorization and harness in [AGENTS.md](../AGENTS.md) and [screenshots.md](screenshots.md). |
| Updating a row | Preserve its ID. Record the final fix, the check actually performed, and the remaining limitation. Add a dated evidence row below; do not convert estimates into measurements. |

The default screenshot harness caps rendering at 30 FPS. It can validate correctness and report frame costs at that cap, but it cannot establish a 60 FPS result. Any different performance configuration must be an explicit, bounded extension of that harness. Do not create an additional game launcher to collect timings.

## Critical paths and cadence

| Path | Call chain / baseline work | User-visible consequence |
| --- | --- | --- |
| Client receive frame | [`transport._process`](../mod/transport.gd) → `_receive_wire` → synchronous `received.emit` → [`main._received`](../mod/main.gd) / `_received_table` → validation → native replica/shop updates. | Packet decoding, reconciliation, native resource creation, and UI work all occur on the same frame path. Receiving more samples can delay drawing even when network latency is low. |
| Table update | `main._process` → `_publish_snapshot` → [`table_sync.capture`](../mod/table_sync.gd) → room relay → `table_sync.apply_snapshot` → [`replica_game.apply_table`](../mod/replica_game.gd). | Baseline snapshots run every 0.10 s and include inventory, results, pockets, full item descriptors, and motion. Baseline shop snapshots use the same cadence. |
| Shop command | [`shop_sync._submit`](../mod/shop_sync.gd) → `_table_send` → room host → table leader `handle_request` → native action / save → result → client `apply_result`. | A predicted visual response can be immediate, while the next mutation still waits for host confirmation. Host frame stalls and relay latency both lengthen that wait. |
| Remote-led table | Guest → room host → table leader; reply returns table leader → room host → guest. If the room host leads the table, only the direct request/reply pair is needed. | Remote-led tables add two one-way relay legs and two relay-frame scheduling opportunities. No fixed millisecond penalty can be inferred without measurement. |
| Periodic state | `main._publish_state` every 0.15 s, plus event/targeted sends; `presence.tick` every 0.05 s. | Approximately 6.7 state updates/s/table and 20 cursor updates/s/active player at steady frame cadence, before event-triggered sends. These are configured rates, not measured throughput. |
| Spectator | Relay cache → `table_spectator.apply_snapshot` → 0.12 s interpolation buffer → `tick` every rendered frame. | The spectator intentionally trades 120 ms of presentation delay for interpolation; this is separate from the player's own input latency. |

## Transport and protocol backlog

| ID | Priority / evidence | Bottleneck and code evidence | Impact | Concrete fix / tradeoff | Status | Acceptance / dependencies |
| --- | --- | --- | --- | --- | --- | --- |
| PERF-001 | P1 / V | [`transport._process`](../mod/transport.gd) originally consumed up to 128 ENet packets or 128 packets from each Steam channel in one frame, including every synchronous receiver callback. There was only a packet-count cap. | CPU/render: a packet burst could monopolize a frame. The theoretical wire ceiling was 32 MiB ENet / 64 MiB Steam per frame, not an observed workload. | Receive pump now checks 2 ms / 32 messages / 256 KiB limits between complete messages, with at least one message allowed. Unread messages stay in the native queue. `receive_stats` exposes count/bytes/time/max-handler/budget counters. | Implemented; unmeasured | Parser/style and the embedded 20-check fake-Steam transport-budget probe passed, including retained reliable ordering, burst limits, and channel progress. Real-load queue age remains unmeasured. A whole message/callback can exceed the limits; see PERF-004. |
| PERF-002 | P1 / V | `transport._receive_wire` immediately dispatches every accepted packet. `main._received_table` rejects already-stale IDs, but increasing snapshot IDs in one burst are each fully applied. Presence messages are likewise all decoded and relayed. | CPU/render/network: obsolete intermediate presentation work competes with drawing the newest state. | Add a bounded latest-state mailbox keyed by session, match, table, phase, and stream. Coalesce only replaceable snapshots/presence; preserve commands, results, reliable phase transitions, and shot-start ordering. | Open | A burst of same-phase snapshots causes at most one final reconciliation per frame. Cross-phase/reconnect fixtures must preserve transitions and discard stale generations. Depends on PERF-001 telemetry. |
| PERF-003 | P2 / V | Steam originally drained reliable then transient channel batches; ENet consumes native FIFO. No per-peer quota exists in `transport._process`. | CPU/network/interaction: one busy stream or sender can consume the receive budget and delay another player's command. | Steam now fetches one message at a time with persistent 3 reliable : 1 transient weighting and empty-channel fallback. ENet remains FIFO because its receive API does not select an inbound channel. | Partial; unmeasured | Parser/style and synthetic queue fairness assertions passed inside the native harness. Still open: per-peer quotas, queue-age measurement, and ENet fairness. Reliable/transient traffic must both progress under sustained load. |
| PERF-004 | P1 / V + H | `transport._process` calls native `_enet.poll()` / Steam `run_callbacks`; `_receive_wire` calls `bytes_to_var` and a complete synchronous handler. A handler can instantiate a scene or save a run. | CPU/render: receive-loop limits cannot interrupt one expensive call. Native cost is unmeasured. | Time decode, dispatch, native polling, and apply separately; reduce/batch the expensive handler before adding threads. Stage scene construction only if native ownership and transition semantics permit it. | Open | Publish p50/p95/p99/max for each stage and worst single-handler duration. A 2 ms pump limit must never be described as a 2 ms hard frame guarantee. |
| PERF-005 | P2 / V | `transport._send_wire` duplicates the envelope and `var_to_bytes` serializes the complete message for each recipient. `broadcast_except` and relay fan-out repeat this. Per-peer session tokens make whole-envelope byte reuse unsafe. | CPU/network: cost grows with payload size × recipients, including watchers. | Measure serialization separately. Share immutable application payloads; consider a compact/versioned payload codec or payload/envelope split while retaining authenticated peer sessions. | Open | Record bytes and encode time by message kind and fan-out at 2/4/8 players. Verify all recipients receive their own correct session envelope. Depends on PERF-013. |
| PERF-006 | P2 / V | [`table_router.route`](../mod/table_router.gd) deep-copies every accepted payload before the room host validates/forwards it. Watcher caching and serialization add further copies. | CPU/memory: motion packets repeatedly allocate nested dictionaries/arrays at the room host. | Establish immutable routing ownership, then remove redundant copies at those boundaries; keep a copy wherever later mutation would otherwise cross recipients or cache generations. | Open | Mutation-isolation probes cover sender, routed payload, cache, and recipients. Report reduced copy counts/allocation volume with no routing-authority regression. |
| PERF-007 | P1 / V | `main._route_table` always calls `_forward_watchers`. Each new snapshot is fully validated and deep-copied into `_watch_snapshots`, even when that table has no watchers. | CPU/render: room-host work grows with all tables, not just its own table or active viewers. | Cache an immutable latest snapshot cheaply; do expensive spectator validation at a shared trusted boundary or first use. Preserve immediate watch startup and independent table isolation. | Open | With zero watchers, no additional full spectator validation/copy per snapshot; switching to a table still hydrates its latest valid frame. Depends on PERF-006 ownership and PERF-014 validation contract. |
| PERF-008 | P1 / V | [`table_sync.capture`](../mod/table_sync.gd) includes complete `PlayerInventory`, every ball's item fields, all pockets, results, and movement in every snapshot (up to 128 balls). Dictionary keys repeat over the wire. | CPU/memory/network: static data is allocated, copied, serialized, decoded, and validated at motion cadence. Large transient packets can also be unsuitable for the underlying unreliable transport. | Separate reliable scene/inventory/item revisions from compact motion snapshots; provide a full resync/keyframe and explicit dependencies. Do not simply delete validation or static state. | Open | Measure encoded distributions and send failures; reordered/lost motion never applies against an unknown inventory/scene revision. Keep bounded payloads below tested transport limits. Depends on PERF-013 and phase tests. |
| PERF-009 | P1 / V | Baseline `main._process` publishes table snapshots every 0.10 s even during a stationary shop. Native client table/HUD work continues behind the shop. | CPU/render/network: redundant work competes with shop animation and input. | Lower ordinary shop table refresh to 0.50 s while keeping shop changes, phase transitions, explicit resyncs, and settled-shot keyframes immediate. Treat further idle suppression as a separate task. | Implemented; unmeasured | Ordinary shop snapshots now use 0.50 s; reliable phase changes and explicit resync remain immediate. Native host/client transition and reordered-update replay passed; packet cadence and bytes still need measurement. |
| PERF-010 | P2 / V | `main._publish_state` sends a reliable full gameplay state every 0.15 s even when unchanged. Shop state is already sent only on change. | CPU/network/interaction: unnecessary reliable bytes share the command path. | Add an unchanged-state guard with a bounded keepalive/resync policy; publish authoritative transitions and acknowledgements immediately. Keep display time separate if it defeats equality checks. | Open | Idle shops/tables emit no redundant reliable state flood; turn, finish, disconnect and spectator updates remain prompt. |
| PERF-011 | P1 / V + H | Reliable commands, state, shop results, and reliable phase snapshots share ENet channel 0 / Steam channel 47. Current wire maximum is 256 KiB. Native queuing and delivery ordering can delay small messages behind large reliable work. | Network/interaction: head-of-line delay under loss or backlog, independent of rendering. Actual Steam behavior needs measurement. | First reduce redundant reliable traffic and oversized keyframes; measure queue age. Only add a separate command channel with explicit cross-channel phase sequencing and resync rules. | Open | Under approved delayed/lossy delivery, report shop-result age and command p95. Phase-before-action correctness must survive any channel separation. Depends on PERF-008/010/013. |
| PERF-012 | P1 / V | `main._table_send` always routes remote table traffic through the room host, even when sender and table leader are on a different table. | Network/interaction: the shop mutation gate waits for a relayed round trip; room-host stalls affect every table. | Retain relay simplicity initially; measure direct vs remote-led tables. A direct authenticated table channel is a larger protocol change requiring membership, reconnect, and authority design. | Open | Record per-leg and end-to-end latency for both topologies; compare improvements against complexity before changing routing. Depends on PERF-013 and PERF-034. |
| PERF-013 | P1 / V | Existing probes assert behavior but do not maintain protocol/render latency histograms, per-kind byte rates, coalesced counts, or command ages. Ping/pong currently supports liveness, not an RTT estimate. | Diagnosis gap: FPS, host load, network delay, and command locking cannot be separated from a symptom alone. | Basic per-pump counters now exist in `transport.receive_stats`. Still add bounded, opt-in aggregate timing and stage histograms inside the existing harness; avoid per-frame logs, disk writes, or user identifiers. | Partial; unmeasured | Attach baseline and post-change artifacts with renderer, cap, fixture, topology, payload sizes, frame p50/p95/p99/max, local feedback vs host confirmation. Current counters alone do not provide those measurements. |

## Client engine and presentation backlog

| ID | Priority / evidence | Bottleneck and code evidence | Impact | Concrete fix / tradeoff | Status | Acceptance / dependencies |
| --- | --- | --- | --- | --- | --- | --- |
| PERF-014 | P1 / V | `main._received_table` validates `snapshot`/`shot_start` through `table_sync._valid_snapshot`, then `table_sync.apply_snapshot` validates the same nested state again. Relay watcher handling may add another pass. | CPU/render: duplicate O(balls + inventory + pockets) validation in one receive callback. | Make one validated boundary explicit, with an internal apply method that cannot be reached from unvalidated network data. Keep public apply safe for independent callers/tests. | Open | Malformed states are rejected once before native mutation; all fixture/standalone entry points remain safe. Do not introduce a caller-controlled “trusted” flag on network data. |
| PERF-015 | P1 / V + N | `replica_game.apply_table` originally called native score, money, round, HP, and floating-time updates every snapshot. Native `table.gd` formats/writes text; `hp_info.gd` iterates preexisting hearts and writes state (it does not recreate heart nodes). | CPU/render: repeated formatting, loops, and UI invalidation. | Cache each UI method's actual input tuple and locale; hydrate once, then call only on meaningful change. Native heart animations already guard full/broken transitions. | Implemented; unmeasured | Parser/style/lint and native replica hydration assertions passed. Identical state must cause no repeated static UI update calls; changed inputs and locale still render correctly. |
| PERF-016 | P2 / V | `replica_game._update_pockets` originally called `set_shield` and `update_label` for every pocket every snapshot; extra-score labels already had a change guard. | CPU/render: unnecessary native updates for 6–16 pockets. | Update shields and multiplier labels on input changes, including first hydration. Retain pocket ordering and hole creation/removal. | Implemented; unmeasured | Parser/style/lint and native hydration/snapshot fixtures passed. Unchanged pocket arrays are skipped; exhaustive locale and changed shield/multiplier call-count coverage remains open. |
| PERF-017 | P1 / V + N | `replica_game.apply_table` originally set `body.sleeping = false` for every replicated ball every snapshot, including stationary/frozen cases. `_physics_process` performs correction and native anti-tunnel calls for unfrozen bodies. | CPU/physics/render: idle bodies can remain artificially active. Native anti-tunnel routines scan wall shapes or ball targets; see PERF-042. | Removed unconditional wake; sleeping bodies without corrections skip collision checks. Motion/force samples wake deliberately. Preserve active-shot prediction and correction. | Implemented; unmeasured | Parser/style/lint and native stationary-sleep, moving/forced-wake, and shot-start assertions passed. High-speed/corner collision coverage and timing remain open. |
| PERF-018 | P2 / V | `replica_game.apply_table` repeatedly writes body freeze, collision shape, velocity, damping, mass, transforms, visibility, and material rotations at sample cadence. Some values are static; shader/physics setters cross engine boundaries. | CPU/render/physics: repeated property writes may dirty native state. | Guard static body properties and unchanged shader basis; retain continuous motion/correction updates where needed. Prioritize expensive setters over speculative micro-optimization. | Implemented; unmeasured | Live body properties and cached shader basis are guarded; parser/style/lint passed. Native spin hydration assertions passed. Native per-frame ball visuals still write shader parameters (PERF-040). |
| PERF-019 | P2 / V + H | `_create_ball` calls `_set_item` before add/initialization and again after `ball_init`; `_set_item` duplicates the flash material and applies native item/weight/effect setup. | CPU/memory/render: spawn bursts and item changes allocate native resources. The two initial calls may be required by native initialization. | Inspect inherited initialization before removing either call; retain existing per-item equality guard. Cache/reuse materials only where they are safely per-instance. | Open | New/mixed/leveled/effect-bearing balls render correctly and resource counts do not grow on repeated state. Requires native source/authorized scene evidence. |
| PERF-020 | P2 / V | [`PlayerInventory.apply`](../mod/player_inventory_sync.gd) reconstructs all nonempty `BallItem` resources and arrays when any inventory field changes. `replica_game` already skips it when the whole inventory is equal. | CPU/memory: a ticket or one-slot change rebuilds otherwise unchanged inventory. | Diff slot identity/content and separate ticket counters; preserve native typed arrays and reference expectations. Combine with wire revisions only after correctness is explicit. | Open | One-slot/ticket updates preserve unrelated `BallItem` identities and native shop layout; full first hydration/resync remains correct. Depends on PERF-008 if wire schema changes. |
| PERF-021 | P1 / V + H | `table_sync.apply_snapshot` creates a full native `SCENE_GAME`, enumerates exported properties, swaps scripts, frees most children in `prepare_scene`, and creates the replica table synchronously when scene key changes. `_clear_replica` synchronously frees the tree. | CPU/render: first join/round/scene transitions can have one-frame stalls even after steady-state fixes. Native macOS compatibility-renderer logs confirm BPTC-to-RGBA8 texture fallback during resource loading. Resource conversion/shader upload costs are unmeasured. | Profile transition stages; reuse persistent scene/UI resources or stage construction behind the existing transition when justified. Do not split ownership across threads blindly. | Open | Report worst transition-frame cost and allocation count; repeated rounds do not leak nodes/resources, lose cosmetics, or flash a partial table. |
| PERF-022 | P1 / V | [`table_spectator.tick`](../mod/table_spectator.gd) updates pockets, text/HUD, hearts, shader fill values, and layout every render frame. `_update_table_ui` / `_update_pockets` repeatedly use recursive `find_child`; only shot pips are already change-guarded. | CPU/render: static metadata work runs at display rate rather than snapshot/change rate. | Cache node references; run metadata/pocket changes once per new state and layout on viewport/bounds changes. Keep only actual interpolation at frame rate. | Open | Identical frames do no text/tree searches or static HUD work; resize, score/HP/pocket changes and interpolated balls remain correct. Extend `spectator_fixtures.gd`. |
| PERF-023 | P2 / V + H | Watching adds a second world in a CanvasLayer; the local table/guest replica continues receiving/applying updates and may simulate beneath it. A table leader must keep authoritative gameplay running. | CPU/physics/render: spectator overhead adds to the player's own table work. Occluded draw savings depend on renderer behavior. | Suspend only unnecessary local presentation while watching; preserve authoritative simulation, networking, phase tracking, and fast return. Consider applying latest replica state on return. | Open | Own table advances correctly while watching; return hydrates immediately with no missed result/shop transitions. Measure host and guest separately. |
| PERF-024 | P2 / V | [`spectator_scene`](../mod/spectator_scene.gd) recursively rebuilds visual trees and duplicates materials; `_refresh_cosmetics` traverses/copies native visuals and stickers. | CPU/memory/render: watch-switch/cosmetic-change spikes, not an every-frame baseline. | Cache immutable visual scene templates/references and isolate mutable materials; optimize only if transition timing justifies it. | Open | Switching tables/cosmetics preserves textures and isolated materials with bounded resource counts and measured lower construction cost. |
| PERF-025 | P2 / V | `main._update_hud` originally cleared both labels then assigned final text every frame. It also computes readiness/membership each frame. | CPU/render: avoidable text invalidation and formatting. | Compute final strings and write only when changed. Cache further derived data only if profiling identifies it. | Implemented; unmeasured | Final HUD text is assigned only when different. Native turn/shop/finish/spectator and result rendering fixtures passed. Call-count and time savings remain unmeasured. |
| PERF-026 | P1 / V | `main._process` originally calls `_render_lobby` every second while hidden. [`lobby_scene._build_tables`](../mod/lobby_scene.gd) clears and recreates cards/controls when rendered. Roster/summary changes also rendered while hidden, including race-time updates. | CPU/render/memory: periodic control-tree churn during gameplay/shop; visible lobby work remains after the hidden-path fix. | Both periodic and `_roster_changed` rendering now require a visible panel. Opening explicitly hydrates the current lobby. Later update visible cards only when content changes. | Partial; unmeasured | Hidden periodic/roster/summary rebuilds removed in code. Verify opening immediately shows current roster/results; visible unchanged periodic/incremental rendering is the remaining task. |
| PERF-027 | P3 / V | [`game_adapter._process`](../mod/game_adapter.gd) polls player hooks/control readiness and settled state each frame; `main` and snapshot/state construction call `game_data`, `is_settled`, and readiness again. | CPU: duplicate lookups/derived checks; likely smaller than scene reconciliation, magnitude unmeasured. | Cache per-frame derived state where lifecycle rules permit. Use hook events for identity changes. Do not weaken input, popup, or settle-time gates. | Open | Control/settlement semantics unchanged; counters justify the cache and demonstrate fewer repeated expensive native checks. |
| PERF-028 | P2 / V | The removed custom multiplayer-ball service scanned host balls and built ability display data every frame. | CPU/memory: removed service and ability-overlay work no longer exists. | Removed custom-ball scripts, assets, shop injection, Bounty scoring, and protocol fields. Native balls remain authoritative. | Retired: feature removed | Verify no custom IDs, resources, call handlers, or ability fields remain in shipped code; native ball/turn/shop fixtures replace feature-specific probes. |
| PERF-029 | P3 / V | [`presence.tick`](../mod/presence.gd) sends at 20 Hz even for an unchanged/hidden cursor state; redraws every active frame; draws labels with repeated font measurement. Relay serializes per teammate. | CPU/network/render: continuous small traffic and draw work even when little changes. | Send on meaningful changes with a keepalive below the 1.5 s stale timeout; cache name widths; stop redraw when no visible interpolating/fading remotes. | Open | Stationary cursors remain visible via keepalive; movement, target snapping, fade, focus loss, and table isolation remain correct. Quantify bandwidth before prioritizing. |

## Shared-shop responsiveness backlog

| ID | Priority / evidence | Bottleneck and code evidence | Impact | Concrete fix / tradeoff | Status | Acceptance / dependencies |
| --- | --- | --- | --- | --- | --- | --- |
| PERF-030 | P1 / V | [`shop_sync._process`](../mod/shop_sync.gd) originally called `_bind_native_items` every frame, scanning four item containers and all bound nodes despite the existing `node_added` hook. | CPU/render: O(shop items) polling while dragging or animating. | Bind on view hydration and node readiness; retain explicit stale-binding cleanup at safe lifecycle boundaries. | Implemented; unmeasured | Per-frame full scan removed; view hydration and node readiness bind items. Native host/guest ball and snack drag, purchase, cancellation, and teardown fixtures passed. Exhaustive reroll/mix/new-node coverage remains open. |
| PERF-031 | P1 / V | `_update_actions` originally called `_cancel_native_drag` every blocked frame. That calls native `drop`, clears inspection, and resets hovered slots. `_interaction_blocked` includes pending network acknowledgement. | CPU/render/interaction: repeated cancellation/hover work while waiting; can make pending actions feel like an unresponsive interface. | Cancel when entering blocked state or changing view, not continuously. Preserve cancellation when a popup, move, finish, or pending action invalidates a live grab. | Implemented; unmeasured | Blocked-state transitions now cancel once. Native live-drag cancellation, view transitions, and delayed/rejected acknowledgement fixtures passed; interaction timing under live load remains open. |
| PERF-032 | P2 / V | [`native_shop._sync_items`](../mod/native_shop.gd) runs only when slots change and already reuses item nodes by ID, but clears/reassigns every slot and updates level sparks/score labels across all surviving items. | CPU/render: one-item changes still touch the whole shop. | Preserve existing node reuse; diff per-slot/item updates, and change level/score visuals only when their inputs change. Avoid losing grabbed/inspected object identity. | Partial; unmeasured | Level/score visuals now update only for changed items; offer prices update only when money/that offer changes. Full slot clearing/reassignment and linear previous-slot searches remain. Native unchanged-offer rarity and item-reuse assertions passed; exhaustive unrelated-live-drag identity coverage remains open. |
| PERF-033 | P1 / V | One guest action can call `shop_sync.capture` before and after `handle_request`, again to build `shop_result`, then again in `_publish_state`. Each capture rebuilds slots, vote context, deep comparisons/copies, and local `apply_state`. Client result can display state more than once, though equality guards avoid some rendering. | CPU/memory/interaction: host work delays the acknowledgement; guest allocates duplicate authoritative/predicted/display dictionaries. | Reuse the final captured state within an accepted/rejected action path while its lobby generation remains valid. Sending can synchronously trigger disconnect/roster callbacks even without an await; refresh when that generation changes. | Partial; unmeasured | Final capture reused by shop_result and _publish_state, with a lobby-generation guard after sending. Typical accepted action goes from four captures to three; validation captures and client copies remain. Controller targeted/broadcast and disconnect-generation regression probes passed; capture counts and timing under real load remain unmeasured. |
| PERF-034 | P1 / V | `_submit` allows one `_pending` action; `_interaction_blocked` disables all shop mutations until a matching result, while prediction only changes display state. | Interaction/network: maximum consecutive mutation rate is limited by the complete request/ack cycle, even at smooth FPS. | Keep financial/state authority intact. First preserve hover, tooltips, navigation and local feedback while a mutation waits. A bounded command queue or per-item reservations needs explicit conflict, idempotency, rollback, and reconciliation rules. | Open | Delayed-ack fixture separately measures first visible feedback, inspection/navigation availability, and next-action eligibility. Concurrent clients cannot double-spend, duplicate, or lose items. Depends on PERF-012/035/037. |
| PERF-035 | P1 / V | `handle_request` rejects non-ready actions unless the entire shop revision equals the host's current revision. Captured busy/readiness changes and unrelated teammate actions can advance that revision. | Interaction: unrelated changes can reject an otherwise valid purchase/reorder and require a retry. | Separate inventory/economy revision from transient UI/readiness state, or validate a command's exact read set (source, target, wallet) atomically. Preserve ready-vote context invalidation on meaningful inventory/economy changes. | Open | Simultaneous disjoint moves can succeed if safe; competing purchase/mix/spend has one authoritative outcome. Stale identities and insufficient shared funds still reject. |
| PERF-036 | P2 / V | `_predict_state` predicts Ready, sales, and selected moves into empty slots; reroll, mix, swaps, and some passive actions wait for authoritative state. | Interaction: feedback varies by action; unpredictable outcomes cannot be safely invented locally. | Add immediate pending/pressed/local animation feedback for all actions. Predict only deterministic, reversible changes; reserve random rolls/mixes and economy decisions for the table leader. | Open | Every click/drop acknowledges locally within the intended frame budget; rejected operations reconcile without flicker or duplicated effects. Extend existing delayed-action fixtures. |
| PERF-037 | P1 / V | Pending timeout is 5 s; it drops prediction, requests a resync, and asks for retry. A state broadcast does not itself establish that a particular request was accepted. Result matching uses request ID on the client; no server-side result cache is present. | Interaction: lost/delayed acknowledgement can keep the whole shop blocked despite current state arriving. Retries need explicit semantics before raising concurrency. | Add host idempotent command IDs/result replay and client acknowledgement/state linkage, then use observed RTT for bounded feedback/recovery. Keep reconnect/session generations explicit. | Open | Late, duplicate, lost, and reordered results/actions produce exactly one authoritative mutation and a bounded recovery; old-session IDs cannot affect a new shop. Depends on PERF-034/035. |
| PERF-038 | P1 / V + N | Accepted `shop_sync.handle_request` calls native `shop.save_run_state()` before returning. Native `ui/shop.gd` updates the build, calls `SaveManager.save_run_state`, then achievement handling. `singletons/save_manager.gd` builds state, deeply duplicates five item arrays, and immediately calls `ResourceSaver.save` for a normal run. | Host CPU/I/O → client interaction: synchronous resource duplication and disk saving are on the acknowledgement path. Time spent remains unmeasured. | Profile native action/save separately. If persistence dominates, coalesce durable saves at safe transaction boundaries with a defined crash-consistency tradeoff; do not remove saving merely to reduce latency. | Open | Verify accepted purchases survive intended save/reload boundaries. Check whether individual native actions also save, to avoid duplicate flushes. Static I/O evidence does not establish it as the dominant delay. |
| PERF-039 | P2 / V | `_ensure_view` may fail `native_shop.ensure_remote_layout`; `_process` retries every frame. Layout setup can repeat while remote items are empty; once items exist, the method refuses a rebuild. | CPU/interaction: malformed timing or capacity changes can cause repeated rebuild attempts or an unusable view. Frequency in real play is unknown. | Retry only when authoritative inventory/layout revision changes; explicitly support or reject capacity changes while open, preserving native item identity. | Open | Host-authoritative larger build, late inventory, and capacity changes either recover once or report a bounded actionable error; no endless per-frame setup loop. Extend `shop_layout_probe.gd`. |
| PERF-040 | P2 / V + N + H | [`native_shop_ball`](../mod/native_shop_ball.gd) / [`native_shop_passive`](../mod/native_shop_passive.gd) invoke inherited processing every item/frame. Native `ShopItem` reads mouse/input and performs per-item hover checks; `ShopBall` animates transforms and writes three rotation shader parameters every frame; passives lerp transforms. Native replica `Ball._process` likewise writes rotation and flash shader parameters and runs selection/size/prediction logic even for sleeping balls. | CPU/render/GPU: native visual/interaction work remains after snapshot guards; shader execution and driver stalls are hypotheses until measured. | Profile native script time, draw calls/resources and GPU time. Cache shared interaction state; suspend invisible decorative work only where input/animation semantics survive. Do not assume snapshot shader guards remove native per-frame writes. | Open | CPU/GPU breakdown with small/full shops, hidden sections, sleeping tables, and effects. No blanket visual disabling or global graphics changes. |
| PERF-041 | P1 / V + N | `native_shop.apply_state` called `inventory.update_info` on money or HP changes. Native `inventory.gd` calls `update_map` → `RoundsMap.set_round` → `initial_setup`, which frees every round pip and instantiates the full map again. Prediction and confirmation can repeat this if displayed money changes. | CPU/render/memory: ordinary purchases/sales can rebuild a decorative map rather than only update the wallet/health. | Call full `update_info` only when round/deck/difficulty changes; use native `inventory.update_money` and `inventory.update_hp` for isolated money/health changes. Add an explicit locale refresh if needed. Preserve first hydration and map changes. | Implemented; unmeasured | Full native update_info is now limited to round/deck/difficulty; isolated changes call inventory.update_money/update_hp. Native assertions confirmed stable map-pip IDs on wallet/HP updates and correct wallet/hearts; round-transition replay also passed. Locale refresh remains a follow-up if supported. |
| PERF-042 | P2 / V + N | `replica_game._physics_process` invokes native anti-tunnel work for unfrozen active bodies. Native `Ball._anti_tunnel_walls` can iterate up to `SUBSTEP_MAX_ITERATIONS`, each calling `GlobalPhysics._raycast_walls` over shapes; cue-ball `_anti_tunnel_balls` scans target balls. Native `Ball._physics_process` normally gates these calls behind a speed threshold; replica uses a different reconciliation flow. | CPU/physics: wall work scales with active bodies × shapes × bounded substeps; cue-ball scan scales with targets. This is not evidence that all-pairs collision simulation dominates. | Measure collision helper time and compare native/replica semantics before applying threshold guards or spatial pruning. Retain high-speed collision and correction behavior. Sleeping skip from PERF-017 already removes an idle case. | Partial; unmeasured | Replica now matches the native supplemental anti-tunnel speed gate (80 squared) and skips sleeping bodies. Static native gates confirmed; high-speed/relative-motion/corner correctness and collision timing still require authorized fixtures. |

## Functional and coverage gaps affecting performance work

| ID | Area | Gap / next concrete action | Status | Acceptance / evidence |
| --- | --- | --- | --- | --- |
| GAP-001 | macOS support | Python 3.9+ installation/launch/uninstall now supports the native app bundle, copied-app signing, Steam discovery, and separate mod saves. Pack/loader requirements were checked statically against the native 0.15.7 installation. | Implemented; local fixtures passed | Thirteen isolated installer tests pass, including stale-owned-file cleanup, recovery, and signing/strict verification of an inert binary that is never executed. The native Mac capture app launched and rendered successfully. Steam joining and mixed-platform sessions remain unverified. |
| GAP-002 | Lobby run choices | Voting now resolves host-available native starting decks/difficulties and room competition mode into a frozen shared run config; stale Ready generations are rejected. Native starters are preserved. Native Daily/Daily Replay are separate modes with date-specific state and are not enabled by difficulty voting. | Implemented; local fixtures passed | Native catalogs/unlock API were inspected statically. The 170-check lobby probe, stale-match controller regression, real native selector/Ready signal cycle, native unlock enumeration, and selected host/guest run hydration passed. Real multiplayer voting still needs a live session. |
| GAP-003 | Performance fixture scale | Existing shop/round/spectator fixtures cover behavior and replay ordering; they do not represent measured 2/4/8-player WAN load or maximum-item scenes. | Open | Add small/large shop, high-ball-count, burst, delayed ack, remote-led table, and active-spectator cases inside existing harness. Label synthetic latency separately from live Steam results. |
| GAP-004 | Runtime verification | Parser and installer checks cannot validate native type/property compatibility, rendering, physical prediction, packet cadence under load, or macOS Steam behavior. | Partial; native fixtures passed | Muted background Mac harness records exit status, gallery, checks, renderer/cap, and original-app/save preservation. Frame distributions, live networking, Windows rendering, and crossplay remain unverified. |
| GAP-005 | Custom multiplayer balls | The custom-ball system, including the Bounty acquisition/reward-window mismatch, has been removed. Native starting sets and shop offers are used throughout. | Retired: feature removed | Native rack, purchase, inspection, and round fixtures validate the remaining game flow; new multiplayer matches always start fresh. Prior custom-ball run files are preserved but unsupported, including native Continue; they are not migrated. |

## Sequenced work packages

| Order | Scope / IDs | Why this order | Deliverable |
| --- | --- | --- | --- |
| 1 | PERF-001/003/009/015–018/025/026/030/031 | Remove bounded, code-evident redundant work while preserving the protocol and one-command authority model. | Small reviewable implementation changes plus parser/static checks; keep status “unmeasured” until runtime evidence exists. |
| 2 | PERF-013, GAP-003/004 | Establish whether remaining pain is frame time, inherited native work, or acknowledgement latency. | Bounded harness instrumentation, measurements, and a revised priority ranking. |
| 3 | PERF-002/006/007/008/010/014/033 | Reduce repetitive validation, copies, reliable traffic, and frame reconciliation using explicit ownership and generation rules. | Burst/order/malformed-input regression coverage, byte/time comparisons. |
| 4 | PERF-034–037 with PERF-011/012 | Improve consecutive shop actions after exactly-once/conflict semantics are defined. | Deterministic concurrent-shop fixtures and local-feedback/confirmation latency results. |
| 5 | PERF-019–024/027–029/032/038–040 | Attack remaining native/visual costs using measured evidence and preserve presentation quality. | Focused optimizations with resource/frame metrics and visual review. |

## Evidence log

| Date | Evidence | Outcome | What it does not prove |
| --- | --- | --- | --- |
| 2026-09-26 | Repository/static native audit across transport, routing, snapshots, replica physics, inventory, native shop, UI, saving, and spectator code. | 42 stable performance IDs and five functional gaps. Native 0.15.7 tokens confirm map-pip reconstruction, synchronous saving/resource duplication, per-item input/shader work, collision scans, and existing native guards. No native scripts are redistributed. | A measured ranking, Windows-pack equivalence, GPU limits, or completeness of every engine/network bottleneck. |
| 2026-09-26 | Static/package and isolated installer checks. | All 43 remaining GDScript files parse. Thirteen macOS installer tests and eight capture-runner lifecycle tests pass without launching their inert fixtures. Python/shell checks, documentation links, source ZIP integrity/executable modes, and whitespace checks pass. | Windows PowerShell fixtures were added/reviewed but not executed because PowerShell was unavailable. Installer fixture signing does not establish Steam connectivity. |
| 2026-09-26 | Native Mac `Capture-Screens.py`, Ultrapool 0.15.7, compatibility renderer, 1280×720, 30 FPS cap, Dummy audio, one background process and fresh isolated profile. | 399 capture checks and all eight embedded probes passed, producing 45 screenshots (run `20260926T114144Z-58b8d1fa`); gallery and logs record voting, native starters, host/client shops, delayed/rejected actions, map identity, rarity retention, sleep/spin, payout/Continue, victory/defeat, and spectator rendering. Original app and normal saves hash-identical; process stopped and temporary app removed. | Background forced rendering is correctness evidence, not a foreground-performance benchmark. No 2/4/8-player WAN/Steam session, frame-time distribution, RTT comparison, or FPS/latency improvement is claimed. |

Capture output contains `runner.json`, `report.json`, `stdout.log`, `stderr.log`, and `index.html` under the git-ignored `.local/screenshots/<run-id>/` directory. The [shared harness](screenshots.md) reproduces these checks; local artifacts are not shipped in the mod ZIP. Performance items stay open until their measurement and remaining coverage criteria pass.

This tracker covers every distinct candidate found in the inspected code paths. It cannot certify that every bottleneck in the game engine or a live network session has been found without profiling those systems.
